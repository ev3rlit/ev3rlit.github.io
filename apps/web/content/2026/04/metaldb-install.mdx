---
title: "VMware Fusion(Mac)에서 MetalLB 설치하기"
date: "2026-04-07"
description: "Mac의 VMware Fusion 위에서 쿠버네티스 실습 환경을 구성할 때 LoadBalancer 서비스 타입을 쓰려면 MetalLB가 필요하다. VMware Fusion DHCP와 IP 충돌 없이 MetalLB를 설치하고 동작을 확인하는 전 과정을 정리한다."
tags: ["Kubernetes", "MetalLB", "VMware Fusion", "LoadBalancer", "TIL", "Mac"]
draft: false
---

{/*
  [REVIEW] 제목/태그
  - 제목 OK. 검색 의도와 잘 맞음.
  - tags: "Network", "IPVS" 추가 고려. LoadBalancer는 너무 일반적이라 태그 효과가 낮을 수 있음.
  - description은 좋은데 "전 과정을 정리한다" 보다 "삽질 포인트인 DHCP 충돌까지 포함해서" 같이
    차별점을 한 문장 더 넣으면 클릭률이 올라갈 것 같음.
*/}

> MetalLB 설치 과정은 단순히 `kubectl apply` 한 줄로 끝나지 않습니다. VMware Fusion DHCP 설정 변경, IPVS strictARP 확인, IP 풀 설정까지 여러 단계가 맞물려 있어 처음에는 어디서 막혔는지 파악하기도 쉽지 않습니다. 나중에 다시 설치할 때 같은 곳에서 헤매지 않으려고 전 과정을 기록해둡니다.

## 들어가며: 왜 MetalLB가 필요한가?

쿠버네티스에서 `LoadBalancer` 타입 서비스를 만들면 `EXTERNAL-IP`가 `<pending>` 상태로 멈춥니다.

```
NAME                 TYPE           CLUSTER-IP      EXTERNAL-IP   PORT(S)        AGE
nginx-loadbalancer   LoadBalancer   10.101.16.141   <pending>     80:32534/TCP   5m
```

클라우드 환경(AWS, GCP 등)에서는 클라우드 프로바이더가 자동으로 IP를 할당해주지만, Mac의 VMware Fusion 위에 구성한 실습 환경에서는 IP를 할당해줄 주체가 없기 때문입니다.

{/*
  [REVIEW] 들어가며 - 좋음 / 보강 포인트
  - pending 화면으로 시작하는 것 좋음. 독자가 바로 "내 얘기네" 하고 공감할 수 있음.
  - 보강: MetalLB가 ARP로 동작한다는 언급이 여기서 살짝 나오는데,
    "Layer 2 (ARP) 모드로 설치한다" 처럼 이 글의 범위(BGP는 다루지 않는다)를
    명시해주면 독자 기대치 관리에 좋음.
  - ✅ MetalLB GitHub 링크 및 소개 추가됨. Layer 2 모드 범위 명시도 완료.
*/}

**[MetalLB](https://github.com/metallb/metallb)** 는 이 역할을 대신합니다. 베어메탈 쿠버네티스 클러스터에서 `LoadBalancer` 타입 서비스를 사용할 수 있도록 표준 라우팅 프로토콜(ARP/BGP)을 이용해 외부 IP를 광고해주는 로드밸런서 구현체입니다. 지정한 IP 풀에서 IP를 선택해 서비스에 할당하고, 해당 IP에 대한 ARP 응답을 직접 수행합니다.

이 글에서는 Layer 2(ARP) 모드로 설치합니다. BGP 모드는 다루지 않습니다.

---

## 설치하기에 앞서

이 글의 실습 환경은 다음과 같습니다.

| 항목 | 내용 |
|---|---|
| 호스트 OS | macOS Apple Silicon(VMware Fusion) |
| 게스트 OS | Rocky Linux |
| MetalLB 버전 | v0.15.3 |
| 네트워크 | vmnet2 (`10.10.10.0/24`) |

## 사전 작업: VMware Fusion DHCP 충돌 방지

MetalLB를 바로 설치하기 전에 **VMware Fusion DHCP와의 IP 충돌**을 막아야 합니다.

### 왜 충돌이 생기는가?

두 주체가 같은 IP 대역에 다른 방식으로 접근합니다.

| | VMware DHCP | MetalLB |
|---|---|---|
| **역할** | VM 부팅 시 IP 자동 할당 | 서비스에 IP 선점 후 광고(announce) |
| **동작 방식** | L3 (IP 계층) | Layer 2 (ARP) 또는 BGP |
| **IP 관리** | 중앙 DHCP 서버가 동적 분배 | 미리 정의된 IP Pool에서 선택 |

MetalLB가 `10.10.10.240`을 선점하고 ARP 응답을 시작했는데, VMware Fusion DHCP가 같은 IP를 다른 VM에 할당하면 충돌이 발생합니다.

{/*
  [REVIEW] DHCP 충돌 설명 - 이 글에서 가장 가치 있는 섹션
  - 비교 테이블 좋음. 두 주체를 나란히 놓으니 차이가 명확하게 보임.
  - 보강 포인트 (중요): 충돌이 실제로 어떤 증상으로 나타나는지가 빠져 있음.
    "충돌하면 어떻게 되냐?" - 이 답이 없으면 독자가 충돌의 심각성을 와닿지 않을 수 있음.
    예시:
      - 같은 IP로 두 호스트가 응답 → 패킷이 랜덤하게 어디로 갈지 모름
      - ping은 되는데 curl이 간헐적으로 실패
      - MetalLB speaker 로그에 ARP 관련 경고
    직접 겪었다면 실제 증상을 짧게 넣어주면 설득력이 크게 올라감.
*/}

### DHCP 범위 줄이기

실습 환경에서 VMware의 vmnet2 서브넷을 `10.10.10.0/24`로 구성했습니다. MetalLB에는 이 대역 중 `240-250` 구간을 IP 풀로 할당할 예정이므로, VMware DHCP가 해당 구간에 IP를 나눠주지 않도록 범위를 먼저 줄입니다.

Windows의 VMware Workstation은 GUI 메뉴(Virtual Network Editor)에서 DHCP 범위를 직접 수정할 수 있지만, Mac의 VMware Fusion은 이 메뉴가 없습니다. 대신 `dhcpd.conf` 설정 파일을 직접 수정해야 합니다.

```bash
vi "/Library/Preferences/VMware Fusion/vmnet2/dhcpd.conf"
```

`10.10.10.0` 서브넷을 찾아 `range` 끝을 `239`로 변경합니다.

```conf
# 변경 전
subnet 10.10.10.0 netmask 255.255.255.0 {
    range 10.10.10.128 10.10.10.254;
    option routers 10.10.10.2;
}

# 변경 후
subnet 10.10.10.0 netmask 255.255.255.0 {
    range 10.10.10.128 10.10.10.239;
    option routers 10.10.10.2;
}
```

{/*
  [REVIEW] DHCP 설정 변경
  - 설정 파일 경로가 VMware Fusion Mac 기준이라는 걸 명시하면 좋음.
    Windows나 Linux VMware Workstation 사용자는 경로가 다름.
  - vmnet2를 왜 쓰는지 한 줄 설명이 있으면 좋음. ✅ 서브넷 대역 설명 추가됨.
    이 글만 읽는 독자는 왜 vmnet2인지, vmnet8은 왜 아닌지 궁금할 수 있음.
    필요하다면 "NAT 네트워크로 vmnet2를 별도 생성해서 사용" 정도 언급 추가 고려.
*/}

변경 후 vmnet을 재시작해야 반영됩니다.

```bash
sudo "/Applications/VMware Fusion.app/Contents/Library/vmnet-cli" --stop
sudo "/Applications/VMware Fusion.app/Contents/Library/vmnet-cli" --start
```

### IP 대역 사용 여부 확인

MetalLB에 할당할 IP 대역이 이미 사용 중인지 확인합니다.

```bash
# -sn: 포트 스캔 없이 호스트 활성화 여부만 확인 (Ping Scan)
nmap -sn 10.10.10.240-250

```

위와 같이 아무것도 표시되지 않으면 사용가능한 상태입니다.

```
Starting Nmap 7.92 ( https://nmap.org ) at 2026-04-08 04:35 KST
Nmap done: 11 IP addresses (0 hosts up) scanned in 1.56 seconds
```


만약 이미 있다면 사용중인 IP가 표시됩니다.

```
Starting Nmap 7.92 ( https://nmap.org ) at 2026-04-08 04:34 KST
Nmap scan report for 10.10.10.240
Host is up.
Nmap scan report for 10.10.10.241
Host is up.
Nmap done: 11 IP addresses (2 hosts up) scanned in 1.58 seconds
```

{/*
  [REVIEW] IP 대역 확인
  - 보강: "응답이 없으면 OK"라고만 하면 부족함. 실제 출력 예시를 보여주면 좋음.
    응답 있는 경우 vs 없는 경우 출력이 어떻게 다른지.
  - ping -c 1은 macOS 기준. Linux VM 안에서 실행한다면 동작하지만,
    Mac 터미널에서 실행하는 경우라면 독자가 어디서 실행하는지 맥락 명시가 필요함.
*/}

---

## MetalLB 설치

### 1. IPVS strictARP 확인

실습 환경은 kube-proxy를 IPVS 모드로 구성했습니다. 이 경우 `strictARP`를 `true`로 설정해야 합니다.

> IPVS 모드는 서비스 IP들을 가상 인터페이스(`kube-ipvs0`)에 등록하는데, Linux 기본 동작은 어느 인터페이스로 ARP가 들어오든 자신이 아는 IP면 다 응답합니다.
> `strictARP=true`는 이를 막아서 MetalLB speaker가 LoadBalancer IP의 ARP 응답을 독점할 수 있게 합니다.

```bash
kubectl get configmap kube-proxy -n kube-system -o yaml | grep strictARP
```

`false`라면 직접 수정합니다.

```bash
kubectl edit configmap -n kube-system kube-proxy
```

```yaml
# 아래 두 값을 확인/수정
strictARP: true
mode: "ipvs"
```

### 2. MetalLB 설치

```bash
kubectl apply -f https://raw.githubusercontent.com/metallb/metallb/v0.15.3/config/manifests/metallb-native.yaml
```

{/*
  [REVIEW] MetalLB 설치 명령어
  - 설치 후 아무 출력도 보여주지 않음. 어떤 리소스들이 생성되는지
    `kubectl apply` 결과를 짧게 보여주면 독자가 "정상적으로 됐구나" 확인할 수 있음.
  - metallb-native vs metallb-frr 차이를 한 줄이라도 언급하면 좋음.
    (native = L2/BGP 기본 모드, frr = BGP 고급 모드)
    이 글에서는 native를 쓰는 이유가 명시되지 않음.
*/}

### 3. ipvsadm 설치

모든 클러스터 노드에 `ipvsadm`을 설치합니다.

```bash
dnf -y install ipvsadm
```

{/*
  [REVIEW] ipvsadm
  - ipvsadm이 뭔지 설명이 없음. "IPVS 규칙을 조회/관리하는 CLI 도구" 한 줄이면 충분.
  - MetalLB 동작에 직접 필요한 건 아니고 디버깅/확인 용도라면 그 용도를 명시.
  - dnf 기반이라 RHEL/Rocky/CentOS 계열 가정. 실습 환경 OS를 앞에서 한 번도 언급하지 않았음.
    어떤 OS를 쓰는지 들어가며 혹은 첫 설치 단계에서 한 줄 언급 필요.
*/}

### 4. 설치 확인

MetalLB는 설치 시 `metallb-system` 네임스페이스를 자동으로 생성하고 모든 구성 요소를 그 안에 배포합니다. 컨트롤러와 스피커 파드가 모두 `Running` 상태인지 확인합니다.

```bash
kubectl get all -o wide -n metallb-system
```

```
NAME                            READY   STATUS    RESTARTS   AGE
pod/controller-cd8c9874-nbqfh   1/1     Running   0          31m
pod/speaker-cxrl2               1/1     Running   0          29m   # node1
pod/speaker-k5gl8               1/1     Running   0          31m   # controller
pod/speaker-qlcgn               1/1     Running   0          29m   # node2
```

`speaker`는 DaemonSet으로 배포되어 모든 노드에 하나씩 올라갑니다.

{/*
  [REVIEW] 설치 확인 출력
  - controller와 speaker의 역할 차이가 없음. 독자가 왜 두 종류인지 모를 수 있음.
    - controller: IP 할당 결정을 담당
    - speaker: 각 노드에서 ARP/BGP 광고를 담당
    이 한 줄이 있으면 "왜 speaker가 노드마다 하나씩이지?" 의문이 해소됨.
  - 처음 설치 직후에는 파드가 Pending → ContainerCreating → Running 단계를 거침.
    바로 Running이 안 되면 당황할 수 있으니 "잠시 기다리면 Running이 된다" 한 마디면 충분.
*/}

---

## MetalLB 배포 설정

MetalLB가 사용할 IP 풀과 L2 광고 설정을 YAML로 정의합니다.

```yaml
# metallb.yaml

# LoadBalancer가 사용할 IP 주소 대역
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: first-pool
  namespace: metallb-system
spec:
  addresses:
    # 이번에 사용할 IP 대역
    - 10.10.10.240-10.10.10.250
---
# IP가 L2(ARP)에서 응답할 수 있도록 설정
apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: default
  namespace: metallb-system
spec:
  ipAddressPools:
    - first-pool
```

{/*
  [REVIEW] metallb.yaml - 전반적으로 잘 작성됨
  - 보강: IPAddressPool만 적용하고 L2Advertisement를 빠뜨리면 어떻게 되는지 언급하면 좋음.
    → IP는 할당되지만 외부에서 ARP 응답이 없어서 실제로 접근이 안 됨.
    이 "흔한 실수"를 한 줄 넣어주면 글의 실용 가치가 높아짐.
  - L2Advertisement에 ipAddressPools를 지정하지 않으면 모든 풀에 적용된다는 것도
    알면 좋은 정보 (선택적으로 추가).
*/}

```bash
kubectl apply -f metallb.yaml
```

적용하면 `<pending>` 상태였던 서비스에 `EXTERNAL-IP`가 할당됩니다.

```
# 변경 전
nginx-loadbalancer   LoadBalancer   10.101.16.141   <pending>      80:32534/TCP   19m

# 변경 후
nginx-loadbalancer   LoadBalancer   10.101.16.141   10.10.10.240   80:32534/TCP   21m
```

---

## 동작 확인

### 로드밸런싱 테스트

테스트용 nginx deployment를 배포합니다. `nginxdemos/hello` 이미지는 요청을 받은 파드의 이름과 IP를 응답 본문에 포함하기 때문에 로드밸런싱 확인에 적합합니다.

```yaml
# nginx-hello.yaml

apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-hello
  namespace: default
  labels:
    app: nginx
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: nginxdemos/hello
```

```bash
kubectl apply -f nginx-hello.yaml
kubectl get pods
```

```
NAME                           READY   STATUS    RESTARTS   AGE
nginx-hello-64d8c497c8-ckwkw   1/1     Running   0          23m
nginx-hello-64d8c497c8-kztvm   1/1     Running   0          51s
nginx-hello-64d8c497c8-wvzgk   1/1     Running   0          51s
```

LoadBalancer 타입 서비스를 배포합니다. MetalLB가 이 서비스를 감지하고 IP 풀에서 주소를 할당합니다.

```yaml
# nginx-loadbalancer.yaml

apiVersion: v1
kind: Service
metadata:
  name: nginx-loadbalancer
spec:
# IP 부여 방식을 LoadBalancer로 지정
  type: LoadBalancer                
  selector:
# Deployment 이름인 nginx와 연동
    app: nginx                      
  ports:
# 포트가 사용하는 프로토콜
  - protocol: TCP                   
    port: 80                        
# 내부 포트(서비스 포트 번호가 여러개일 때)
    targetPort: 80                  
```

```bash
kubectl apply -f nginx-loadbalancer.yaml
```

LoadBalancer에 할당된 EXTERNAL-IP인 `10.10.10.240`으로 `curl`을 여러 번 호출하면 매번 다른 파드로 연결됩니다.

```bash
curl 10.10.10.240 | grep Server
```

`Server name`은 요청이 전달된 파드의 이름입니다. 호출할 때마다 다른 파드 이름이 나오면 로드밸런싱이 정상 동작하는 것입니다.

```
# 첫 번째 요청
<p><span>Server name:</span> <span>nginx-hello-64d8c497c8-wvzgk</span></p>

# 두 번째 요청
<p><span>Server name:</span> <span>nginx-hello-64d8c497c8-ckwkw</span></p>
```

{/*
  [REVIEW] 동작 확인 섹션
  - nginx-hello deployment가 어디서 왔는지 설명이 없음.
    사전에 만들어둔 건지, 이 글에서 처음 만드는 건지 독자가 알 수 없음.
    간단한 nginx deployment 생성 명령어를 앞에 한 블록 추가하거나,
    "앞서 배포해둔 nginx-hello deployment를 사용한다" 한 줄 언급 필요.
  - 로드밸런싱이 매 요청마다 다른 파드로 간다고 했는데,
    실제로는 L2 모드에서는 특정 노드의 speaker가 ARP를 독점 응답하기 때문에
    트래픽이 한 노드로 쏠린 후 kube-proxy가 파드 간 분산을 함.
    완전한 ECMP 로드밸런싱이 아님 — 이 점은 L2 모드의 한계로,
    알고 있다면 한 줄 언급하면 글의 깊이가 생김 (선택 사항).
*/}

---

## 정리

| 단계 | 작업 | 명령어/파일 |
|------|------|--------|
| 사전 | VMware DHCP 범위 축소 | `dhcpd.conf` range 수정 → vmnet 재시작 |
| 사전 | 대역 사용 여부 확인 | `nmap -sn 10.10.10.240-250` |
| 사전 | IPVS strictARP 설정 | `kubectl edit configmap kube-proxy -n kube-system` |
| 설치 | MetalLB 설치 | `kubectl apply -f metallb-native.yaml` |
| 설정 | IP Pool + L2Advertisement | `kubectl apply -f metallb.yaml` |
| 확인 | 네임스페이스 상태 확인 | `kubectl get all -n metallb-system` |

VMware Fusion DHCP와의 IP 충돌만 주의하면 MetalLB 설치 자체는 간단합니다. 이제 `<pending>` 상태로 멈춰있던 `EXTERNAL-IP`에 실제 IP가 할당되고, LoadBalancer 타입 서비스를 활용한 실습을 본격적으로 진행할 수 있는 환경이 마련되었습니다.

{/*
  [REVIEW] 마무리
  - 마지막 문장은 좋음. 개인적인 감상이 글에 온기를 줌.
  - 추가하면 좋은 것들 (선택):
    1. 트러블슈팅 섹션: MetalLB 설치 후 IP가 여전히 <pending>일 때 확인할 것들
       - speaker 파드 로그: kubectl logs -n metallb-system -l component=speaker
       - IPAddressPool이 제대로 적용됐는지: kubectl get ipaddresspool -n metallb-system
    2. 이 글의 실습 환경 명시 (어딘가에 한 번은 필요):
       - OS: Rocky Linux 9 (혹은 실제 사용한 것)
       - k8s 버전, MetalLB 버전
       - 노드 구성: controller 1대 + node 2대
    3. 참고 링크: MetalLB 공식 문서, 이전 쿠버네티스 실습 글 링크

  [총평]
  - 설치 절차 자체는 잘 정리됨. 명령어 흐름이 끊기지 않고 따라갈 수 있음.
  - 가장 큰 보강 포인트: "왜"가 빠진 곳들 (strictARP 이유, speaker/controller 역할, L2 모드 한계)
  - 두 번째 보강 포인트: 실습 환경 컨텍스트 (OS, 버전, 노드 구성)가 글 어딘가에 한 번은 있어야
    독자가 "내 환경과 맞는지" 판단할 수 있음.
  - 분량 적정. 지금 구조에서 "왜" 설명 몇 줄씩만 추가해도 완성도가 많이 올라갈 것 같음.
*/}
