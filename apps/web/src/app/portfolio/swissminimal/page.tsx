
import { SwissMinimalPage } from "@/views/portfolio/swissminimal";
import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Portfolio (Swiss Minimal) | HOMVELOPER",
    description: "Creative Developer Portfolio - Swiss Style",
};

export default function Page() {
    return <SwissMinimalPage />;
}
