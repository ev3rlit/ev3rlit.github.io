import PlaygroundPage from '@/views/playground/ui/PlaygroundPage';

export const metadata = {
    title: 'Playground | Dev Blog',
    description: 'Interactive canvas playground',
};

export default function Page() {
    return <PlaygroundPage />;
}
